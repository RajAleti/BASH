# Syntax 1 (Recommended)
function_name() {
  # Block of code to execute
  echo "This is inside the function."
}

# Syntax 2 (Also common)
function function_name {
  # Block of code to execute
  echo "This is also inside a function."
}

# To run the function, you simply "call" it by its name.
function_name
